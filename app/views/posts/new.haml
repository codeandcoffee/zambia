#post
  %h1.title
    Create a new Post
  #entry
    %fieldset
      - form_for @post, :url => '/posts' do |f|
        = render "form", :form => form, :f => f
        .section
          = submit_tag 'Submit'
